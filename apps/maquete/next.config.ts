import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Maquete mock-first: sem backend, sem imagens remotas, sem variáveis de ambiente.
  // Nada aqui deve introduzir chamada de rede.
}

export default nextConfig
